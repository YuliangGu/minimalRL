# REINFORCE

- `train.py` contains the reference implementation for episodic policy gradients with Monte Carlo returns.
- `theory.md` walks through the policy-gradient theorem and baseline variance reduction.

See `docs/algorithms/reinforce.md` for a high-level overview that links back here.

## Quickstart
```bash
python -m minimalrl.algos.reinforce.train --help
```
